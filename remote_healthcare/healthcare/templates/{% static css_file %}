/* ive set syles natively inside the home page we shall separate later in static*/
body, h1, p {
    margin: 0;
    padding: 0;
}

header {
    background-color: #0054a6;
    color: white;
    padding: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
}

.nav-links li a {
    color: white;
    text-decoration: none;
    font-weight: bold;
    display: flex;
    align-items: center;
    gap: 10px;
}

.main-content {
    padding: 3rem;
    text-align: center;
}

.info h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
}

.info p {
    font-size: 1.2rem;
}

.logo {
    font-size: 1.5rem;
    cursor: pointer;
}
